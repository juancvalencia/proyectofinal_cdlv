//Codificamos con React - Archivo de Arranque

import React from 'react';
import { Component } from 'react';
import ReactDOM, { render } from 'react-dom';

import App from './App';

render(<App/>, document.getElementById('app'));
